{
    "name": "projectino-hardhat",
    "version": "1.0.0",
    "description": "Hardhat testing for ProjectINO NFT contract",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "clean": "npx hardhat clean"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.28.3"
    },
    "dependencies": {
        "dotenv": "^17.2.4"
    }
}
